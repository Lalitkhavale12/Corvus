# --- Core (Phase 1: data pipeline) ---
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3
pyyaml>=6.0
loguru>=0.7

# --- EDA / visualization ---
matplotlib>=3.7
seaborn>=0.12

# --- Modeling (Phase 2) ---
xgboost>=2.0
lightgbm>=4.0

# --- Experiment tracking / registry (Phase 2-3) ---
mlflow>=2.10

# --- Data validation (Phase 1-2) ---
great-expectations>=0.18

# --- API (Phase 3) ---
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
psycopg2-binary>=2.9
streamlit>=1.32
python-multipart>=0.0.18

# --- Testing ---
pytest>=8.0

# --- Later phases (uncomment as needed) ---
# evidently>=0.4          # Phase 8: drift detection
# apache-airflow>=2.8     # Phase 9: retraining orchestration
# prometheus-client>=0.20 # Phase 7: monitoring
