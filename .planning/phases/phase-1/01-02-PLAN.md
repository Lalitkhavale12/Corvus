---
phase: 1-data-pipeline
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - notebooks/01_eda_ckd.ipynb
autonomous: true
requirements:
  - FR-1.2
must_haves:
  truths:
    - "Opening notebooks/01_eda_ckd.ipynb shows per-column distributions, a numeric correlation heatmap, and a missing-value heatmap rendered from the real raw CSV"
    - "Every missingness number displayed in the notebook matches summarize() output exactly (single source of truth)"
    - "The notebook executes top-to-bottom without error in the project interpreter"
    - "The notebook contains no data-mutating or fitting logic and no duplicated quirk handling"
  artifacts:
    - notebooks/01_eda_ckd.ipynb
  key_links:
    - "Notebook first code cell imports load_raw_data, summarize, clean_raw from src.* via a sys.path shim (never re-implements quirk logic)"
    - "Correlation block coerces via pd.to_numeric(errors=coerce) on NUMERIC_COLS imported from src.preprocessing.preprocess"
---

<objective>
Create the read-only EDA notebook that profiles the real CKD raw data through the existing pipeline functions: distributions, numeric correlations, and missingness visualization. The notebook observes and never mutates — it imports quirk handling and cleaning rather than duplicating them, so it can never become a second source of truth.

Purpose: FR-1.2 data-quality insight needs a human-readable artifact the team can open and trust; grounding every number in summarize() and load_raw_data() by construction removes the drift risk where notebook numbers disagree with pipeline numbers.
Output: notebooks/01_eda_ckd.ipynb committed with cleared outputs, executable end-to-end, figures saved to reports/figures/ as runtime outputs.
</objective>

<execution_context>
@C:/Users/Lalit/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Lalit/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-1/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Notebook skeleton plus missingness analysis over summarize()</name>
  <files>notebooks/01_eda_ckd.ipynb</files>
  <read_first>
    - src/ingestion/load_data.py (load_raw_data and summarize signatures and return shapes)
    - src/preprocessing/preprocess.py (NUMERIC_COLS and CATEGORICAL_COLS lists, clean_raw signature)
    - src/utils/config.py (load_config for the config-driven raw_path the notebook must use)
    - config/config.yaml (data.raw_path value the notebook resolves through config, never a literal)
    - .planning/phases/phase-1/01-RESEARCH.md Pattern N1 plus Pitfalls 1 and 4 (read-only rules, sys.path shim, no quirk duplication)
  </read_first>
  <action>Create notebooks/ directory if absent and write notebooks/01_eda_ckd.ipynb with markdown cells stating the read-only contract (imports src functions, never re-implements quirk handling, never fits anything) followed by code cells that: insert parent dir into sys.path then import load_raw_data and summarize from src.ingestion.load_data plus NUMERIC_COLS and CATEGORICAL_COLS from src.preprocessing.preprocess; load eda_df via load_raw_data() with no arguments so the configured raw path is used; compute summary via summarize(eda_df) and display it; render a horizontal missing-percentage bar chart from df.isna().mean().mul(100) sorted descending; render a boolean-mask missingness heatmap via sns.heatmap(df.isna()) with yticklabels False; save both figures to reports/figures/ with bbox_inches tight. Name working copies eda_df or plot_df, never df reassigned through fillna or imputation. Include no .fit, fit_transform, SimpleImputer, StandardScaler, get_dummies, df.replace with a ? pattern, pip shell cells, or os.system calls anywhere in the notebook JSON.</action>
  <acceptance_criteria>
    - notebooks/01_eda_ckd.ipynb exists with at least one markdown contract cell and code cells covering load, summarize display, missingness bar, and missingness heatmap
    - The first code cell imports load_raw_data and summarize from src.ingestion.load_data after a sys.path parent-dir insert
    - No cell contains .fit(, fit_transform, get_dummies, SimpleImputer, ? regex replace, !pip, or os.system
    - Figures are written under reports/figures/ (missingness_heatmap.png plus the bar-chart png) and nowhere else
  </acceptance_criteria>
  <verify>
    <automated>venv/python.exe -c "import json; nb=json.load(open('notebooks/01_eda_ckd.ipynb')); print(len(nb['cells']))"</automated>
    <fails_when>exit code is non-zero, output contains Traceback, or the printed cell count is below 5</fails_when>
  </verify>
  <done>Skeleton plus missingness section exists, is quirk-logic-free, and parses as valid notebook JSON.</done>
</task>

<task type="auto">
  <name>Distributions plus correlation heatmap plus executed-and-cleared commit</name>
  <files>notebooks/01_eda_ckd.ipynb</files>
  <read_first>
    - notebooks/01_eda_ckd.ipynb (skeleton from the previous task to extend in place)
    - src/preprocessing/preprocess.py (clean_raw behavior for the target-aware plot frame: label cleaning to ckd/notckd then binary mapping)
    - .planning/phases/phase-1/01-RESEARCH.md Code Examples section (displot hue/dodge idiom, masked correlation triangle with vmin -1 vmax 1)
  </read_first>
  <action>Extend the notebook from the prior task with two more analysis sections and a closing markdown cell. Distributions section: build plot_df via clean_raw(load_raw_data()) reusing pipeline cleaning, then render a kde displot of hemo split by classification hue and a countplot of rbc split by classification hue. Correlation section: coerce NUMERIC_COLS with pd.to_numeric errors coerce, compute corr with numeric_only True, mask the upper triangle with np.triu ones bool mask, render sns.heatmap with annot True fmt .2f cmap vlag center 0 vmin -1 vmax 1 square True, save to reports/figures/correlation_heatmap.png. Closing cell states the two takeaways the plots must show (highest-missingness columns, strongest correlated numeric pair) as plain text conclusions, not new computations. Then install execution support with venv/python.exe -m pip install jupyter nbconvert (both confirmed absent in refreshed research), execute the notebook top-to-bottom with venv/python.exe -m jupyter nbconvert --to notebook --execute, confirm exit 0, and finally clear all cell outputs before committing so the committed file carries code and markdown only.</action>
  <acceptance_criteria>
    - The notebook contains a hemo kde-by-class plot cell, an rbc count-by-class cell, and a masked correlation heatmap cell with the exact scale parameters vmin -1 vmax 1 center 0
    - plot_df is derived only from clean_raw(load_raw_data()) with no manual label recoding
    - The committed ipynb has zero stored outputs (no outputs array with data in any code cell) and zero traceback strings
  </acceptance_criteria>
  <verify>
    <automated>venv/python.exe -m jupyter nbconvert --to notebook --execute notebooks/01_eda_ckd.ipynb --output C:\Users\Lalit\AppData\Local\Temp\opencode\eda_exec_check.ipynb</automated>
    <fails_when>exit code is non-zero, or stderr contains Error/Traceback, or the output file is not created</fails_when>
    <automated>venv/python.exe -c "import json; nb=json.load(open('notebooks/01_eda_ckd.ipynb')); src='\n'.join(''.join(c.get('source',[])) for c in nb['cells'] if c['cell_type']=='code'); code=[l for l in src.splitlines() if not l.strip().startswith('#')]; bad=[t for t in ['.fit(','fit_transform','get_dummies','os.system'] if t in '\n'.join(code)]; print('violations:', bad); raise SystemExit(1 if bad else 0)"</automated>
    <fails_when>exit code is non-zero, output contains Traceback, or the violations list is non-empty</fails_when>
  </verify>
  <done>Notebook executes cleanly, committed outputs-free, distributions plus correlation sections present, forbidden-logic scan passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw CSV → notebook kernel | Untrusted file content rendered into a human-viewed artifact |
| notebook JSON → git history | Stored outputs or malicious cells would persist for every future opener |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-04 | Elevation | notebooks/01_eda_ckd.ipynb committed outputs | medium | mitigate | Commit with cleared outputs; verify step asserts zero stored outputs |
| T-1-05 | Tampering | notebook as shadow preprocessing path | high | mitigate | Forbid .fit/fit_transform/get_dummies/quirk-regex in notebook; programmatic scan in verify |
| T-1-06 | Elevation | notebook shell execution | medium | mitigate | No !-prefixed shell cells and no os.system; covered by the same scan task |
| T-1-SC | Tampering | venv pip installs | low | accept | Only jupyter plus nbconvert installed into the venv via venv/python.exe -m pip install (both confirmed absent in refreshed research); official Jupyter packages, no new runtime surface |
</threat_model>

<verification>
Notebook parses as JSON, executes via nbconvert with exit 0, committed outputs-free, forbidden-logic scan clean, figures present under reports/figures/ as runtime outputs.
</verification>

<success_criteria>
FR-1.2 is human-verifiable: opening the notebook shows dtype/missingness summary grounded in summarize(), per-column distributions, numeric correlations, and a missingness map, all computed from the configured raw path with zero duplicated quirk logic.
</success_criteria>

<output>
Create `.planning/phases/phase-1/01-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

New files: notebooks/01_eda_ckd.ipynb (read-only EDA: summarize display, missingness bar plus heatmap, hemo kde by class, rbc counts by class, masked correlation heatmap, conclusions cell; committed with cleared outputs).
Runtime outputs (not committed, reports/ is gitignored): reports/figures/missingness_heatmap.png, reports/figures/missingness_bar.png, reports/figures/correlation_heatmap.png.
New symbols: none (notebook only calls load_raw_data, summarize, clean_raw, NUMERIC_COLS, CATEGORICAL_COLS).
</output>
