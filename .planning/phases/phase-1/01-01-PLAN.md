---
phase: 1-data-pipeline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - data/validation/ckd_suite.json
  - data/validation/validate.py
  - src/preprocessing/preprocess.py
autonomous: true
requirements:
  - FR-1.1
  - FR-1.7
must_haves:
  truths:
    - "Running the validation runner against data/raw/kidney_disease.csv exits 0 (raw drop satisfies the persisted contract)"
    - "Running the validation runner against a synthetic bad frame (unknown label, missing target column) exits non-zero without raising an unhandled traceback"
    - "preprocess.main() aborts before clean_raw when validation fails and completes normally when it passes"
    - "All 4 pre-existing pytest tests still pass after the main() wiring edit (no leakage or Claim-2 regression)"
    - "No new code contains a hard-coded row count, hard-coded data path, or a .fit call outside fit_transform_split"
  artifacts:
    - data/validation/ckd_suite.json
    - data/validation/validate.py
  key_links:
    - "validate.py run_raw_validation(df) is called from preprocess.main() between load_raw_data() and clean_raw()"
    - "validate.py and ckd_suite.json both resolve paths through src.utils.config.load_config (data.validation_dir, data.raw_path)"
---

<objective>
Establish the Phase 1 validation gate end-to-end: using the resolved project interpreter venv/python.exe (Python 3.11.0) with installed great-expectations 1.23.1 via the GX 1.x API only (no version branch, no parallel fallback build), persist a raw-CSV expectation suite as JSON, ship a thin runner, wire it fail-fast into preprocess.main(), and prove the whole slice green. This is the phase tracer — it touches ingestion, validation, and preprocessing in one runnable path before the EDA and test-expansion plans build out from it.

Purpose: A validation gate that only exists on paper lets malformed raw drops silently reach training. Proving raw-CSV validation plus an unregressed main() in the first plan uses the resolved interpreter venv/python.exe (Python 3.11.0, GX 1.23.1 verified installed; system Python 3.14 is out of scope per refreshed research) while core code is still untouched by other plans.
Output: ckd_suite.json, validate.py with run_raw_validation (GX 1.x only), fail-fast wiring in preprocess.main(), recorded venv interpreter + GX 1.23.1 smoke evidence.
</objective>

<execution_context>
@C:/Users/Lalit/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Lalit/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-1/01-RESEARCH.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end validation gate slice — interpreter, GX spike, suite JSON, runner, main() wiring</name>
  <files>data/validation/ckd_suite.json, data/validation/validate.py, src/preprocessing/preprocess.py</files>
  <read_first>
    - config/config.yaml (keys data.raw_path, data.processed_dir, data.validation_dir, data.target_column, data.positive_label)
    - src/utils/config.py (load_config lru_cache plus PROJECT_ROOT path anchoring)
    - src/ingestion/load_data.py (load_raw_data signature with raw_path override, quirk regex, summarize columns)
    - src/preprocessing/preprocess.py (main() ordering load_raw_data then clean_raw, save_outputs outputs)
    - requirements.txt (great-expectations>=0.18 floor, pytest>=8.0 floor)
    - .planning/phases/phase-1/01-RESEARCH.md Pattern V1 plus Pitfalls 3 and 6 (GX 1.x only with range-based expectations, venv/python.exe exclusively; Pitfall 2 version-branch retired)
  </read_first>
  <action>Use venv/python.exe exclusively for every command (never bare python/pytest/pip; system Python 3.14 is out of scope) per refreshed research Pitfall 6. Confirm venv/python.exe --version reports Python 3.11.0 plus a one-line GX smoke import against installed great-expectations 1.23.1, and record both outcomes in the plan SUMMARY. Implement the runner against the GX 1.x API only per research Pattern V1 (gx.get_context, add_pandas data source, add_dataframe_asset, add_batch_definition_whole_dataframe, batch.validate(suite) or ValidationDefinition run with batch_parameters dataframe); never use the 0.18 fluent idiom (context.sources.pandas_default, checkpoints). Keep at most a minimal import-guard in validate.py that raises an actionable error message when great_expectations is not importable; do not build a parallel hand-rolled validate_raw validator implementation. Persist data/validation/ckd_suite.json as a version-agnostic list of expectation dicts with expectation_type and kwargs covering: table columns match set with exact_match false including classification, classification value set restricted to ckd and notckd post-strip domain, classification not null, row count between 300 and 500 (range, never an exact count), and missing-fraction upper bounds for the highest-missingness columns taken from summarize() output rounded up. Implement data/validation/validate.py exposing run_raw_validation(df=None) returning an object with a success boolean: when df is None it loads via load_raw_data() with paths from load_config()["data"], it reads the suite JSON from load_config()["data"]["validation_dir"], never hard-codes a data path or row count, never calls .fit, and its __main__ block exits non-zero on failure. Wire the gate into preprocess.main() between load_raw_data() and clean_raw() by loading validate.py through importlib file-location anchored at PROJECT_ROOT plus cfg data.validation_dir (data/ has no package init, so no plain data.validation import), calling run_raw_validation(df), and raising SystemExit with the failure message when success is False. Do not alter clean_raw, split_raw, build_pipeline, fit_transform_split, or save_outputs bodies.</action>
  <acceptance_criteria>
    - venv/python.exe --version (Python 3.11.0) and the GX 1.23.1 smoke-import outcome are recorded in the SUMMARY
    - data/validation/ckd_suite.json exists and contains no exact-400 row assertion and no exact_match true entry
    - validate.py exposes run_raw_validation and resolves all paths via load_config
    - preprocess.main() source contains exactly one validation call site positioned after load_raw_data() and before clean_raw()
    - split_raw, build_pipeline, fit_transform_split, save_outputs function bodies are byte-identical to before (verified by git diff showing only the main() hunk in preprocess.py)
  </acceptance_criteria>
  <verify>
    <automated>venv/python.exe data/validation/validate.py</automated>
    <fails_when>exit code is non-zero on the committed data/raw/kidney_disease.csv, or stderr contains Traceback/ModuleNotFoundError/AttributeError</fails_when>
    <automated>venv/python.exe -m pytest tests/test_preprocessing.py -x -q</automated>
    <fails_when>exit code is non-zero, or output contains FAILED or ERROR, or the 4-test count drops below 4 collected</fails_when>
  </verify>
  <done>The real raw CSV passes the runner with exit 0, the 4 existing tests pass, and main() fails fast on bad data while completing on good data.</done>
</task>

<task type="auto">
  <name>Negative-path proof plus forbidden-pattern sweep</name>
  <files>data/validation/validate.py</files>
  <read_first>
    - data/validation/validate.py (the runner created in the tracer task)
    - data/validation/ckd_suite.json (expectation kwargs the negative frames must violate)
    - src/preprocessing/preprocess.py (main() gate call site to exercise)
  </read_first>
  <action>Prove the gate rejects bad drops by exercising run_raw_validation against three synthetic in-memory frames (no real files): one with an out-of-domain label such as maybe in classification, one with the classification column removed, one with only 2 rows. Assert each returns success False and that a good synthetic frame with ckd/notckd labels and sufficient rows returns success True. Then sweep the two new files for forbidden patterns: no literal 400 row assertion, no hard-coded data/ path string, no .fit( or fit_transform occurrence. Fix any violation in place rather than documenting it.</action>
  <acceptance_criteria>
    - All three bad synthetic frames yield success False and the good frame yields success True
    - A case-insensitive search for fit( and fit_transform across data/validation/ returns zero hits
    - No == 400 or equivalent exact row-count literal exists in ckd_suite.json or validate.py
  </acceptance_criteria>
  <verify>
    <automated>venv/python.exe -m pytest tests/test_preprocessing.py -x -q</automated>
    <fails_when>exit code is non-zero or output contains FAILED or ERROR (regression guard after any runner edit)</fails_when>
    <automated>venv/python.exe -c "import json; s=json.load(open('data/validation/ckd_suite.json')); print(len(s))"</automated>
    <fails_when>exit code is non-zero, output contains Traceback, or the printed suite length is 0</fails_when>
  </verify>
  <done>Bad frames are rejected, good frames pass, forbidden-pattern sweep is clean, and the regression suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw CSV on disk → validate.py | Untrusted third-party-shaped file content crosses into the pipeline here |
| validate.py → preprocess.main() | Gate verdict crosses into execution control (abort vs proceed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-01 | Tampering | data/validation/validate.py gate | high | mitigate | Fail-fast SystemExit before clean_raw on success False; bad frames covered by negative-path task |
| T-1-02 | Tampering | config-driven paths in runner | medium | mitigate | All paths via load_config PROJECT_ROOT anchoring; no bare relative open() calls |
| T-1-03 | Tampering | .joblib artifact load path | low | accept | save_outputs only writes and Phase 2 only reads the pipeline the repo itself fitted; rationale recorded, no third-party joblib ever loaded |
| T-1-SC | Tampering | pip installs | high | mitigate | No new packages introduced by this plan; GX and pytest floors already pinned in requirements.txt |
</threat_model>

<verification>
Runner exits 0 on the real raw CSV, exits non-zero on synthetic bad frames, preprocess.main() ordering is validate-then-clean, and the 4 existing tests pass unmodified.
</verification>

<success_criteria>
FR-1.1 raw contract is enforced by a persisted suite (range-based, strip-aware); FR-1.7 holds because runner and wiring read every path and threshold from config/config.yaml; FR-1.3 through FR-1.6 are regression-guarded by the untouched 4-test suite plus an unchanged split/fit/save code path.
</success_criteria>

<output>
Create `.planning/phases/phase-1/01-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

New files: data/validation/ckd_suite.json (persisted raw-CSV expectation suite, range-based, no exact counts), data/validation/validate.py (run_raw_validation(df) plus non-zero-exit __main__).
New symbols: run_raw_validation(df=None) returning a result object with success bool.
Modified: src/preprocessing/preprocess.py main() only (single gate call site between load_raw_data and clean_raw; all other functions byte-identical).
</output>
