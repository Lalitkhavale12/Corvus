---
phase: 1-data-pipeline
plan: "03"
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - tests/test_ingestion.py
  - tests/test_save_outputs.py
  - tests/test_validation.py
autonomous: true
requirements:
  - FR-1.1
  - FR-1.2
  - FR-1.3
  - FR-1.4
  - FR-1.5
  - FR-1.6
  - FR-1.7
must_haves:
  truths:
    - "A quirky synthetic CSV (?, padded whitespace, tabbed labels, numeric-as-string cells) loads with quirks normalized and no quirk regex duplicated outside load_raw_data"
    - "summarize() output shape and ordering are pinned by a test independent of the real CSV"
    - "save_outputs() writes train/val/test CSVs plus a reloadable .joblib into an isolated tmp dir with row counts summing to the input, and never touches real data/processed/"
    - "The validation runner accepts a good synthetic frame and rejects unknown-label, missing-target-column, and tiny-row-count frames"
    - "The full suite (4 pre-existing plus all new tests) passes in one command with no real-dataset dependency"
  artifacts:
    - tests/test_ingestion.py
    - tests/test_save_outputs.py
    - tests/test_validation.py
  key_links:
    - "Ingestion tests call the real load_raw_data(str(tmp_csv)) and summarize() (never re-implement normalization)"
    - "save_outputs test redirects config via monkeypatched load_config plus cache_clear (lru_cache) to tmp_path"
    - "Validation tests import the runner from data/validation/validate.py via sys.path insert (data/ is not a package)"
---

<objective>
Expand pytest coverage for everything Phase 1 added beyond the 4 pre-existing preprocessing tests: ingestion quirk handling, summarize() shape, config-driven paths, save_outputs() round-trip isolation, and the validation gate accept/reject behavior. All tests use synthetic frames and tmp_path only, assert behavior rather than exact real-data values, and leave data/processed/ untouched.

Purpose: The 4 existing tests guard split ordering and the Claim-2 artifact but nothing guards quirk normalization, output persistence, or the new gate — the three areas most likely to regress silently when Phase 2 starts consuming pipeline outputs.
Output: Three new test files, full suite green, git status clean of data/processed/ modifications.
</objective>

<execution_context>
@C:/Users/Lalit/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Lalit/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/phase-1/01-RESEARCH.md
@.planning/phases/phase-1/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Ingestion quirk tests plus summarize and config-override tests</name>
  <files>tests/test_ingestion.py</files>
  <read_first>
    - src/ingestion/load_data.py (load_raw_data raw_path parameter, quirk regex line, column-strip loop, summarize return columns and sort)
    - src/utils/config.py (load_config path parameter and lru_cache, PROJECT_ROOT anchoring of data.raw_path)
    - tests/test_preprocessing.py (synthetic-frame precedent and import style via tests package init)
    - config/config.yaml (data.raw_path, data.target_column, data.positive_label values the tests must read, not copy)
  </read_first>
  <action>Create tests/test_ingestion.py following the synthetic-frame precedent: write a quirky CSV into tmp_path containing a bare ? cell, a padded " ? " cell, whitespace-padded values (" yes", tab-prefixed no), numeric-as-string cells, and a ckd label with trailing tab; load it with load_raw_data(str(quirky_csv)) and assert ? cells are NA, surrounding whitespace is stripped from values and column names, and the tabbed ckd label normalizes to ckd. Add a summarize test on a small synthetic frame asserting the exact column set dtype/missing_count/missing_pct/n_unique, descending sort by missing_pct, and missing_pct bounded 0 to 100. Add a config-override test proving FR-1.7: write a minimal config yaml into tmp_path pointing data.raw_path at the quirky csv, monkeypatch src.ingestion.load_data.load_config to return the parsed override (calling load_config.cache_clear first because of lru_cache), and assert load_raw_data() with no arguments picks up the override path. Never assert the exact 400-row real file size; never copy the ? regex into the test file.</action>
  <acceptance_criteria>
    - tests/test_ingestion.py contains at least 4 test functions covering ? markers, whitespace/tab stripping, summarize shape, and config override
    - The test file contains no regex literal matching the quirk pattern and no hard-coded data/ path string
    - The config-override test calls load_config.cache_clear or monkeypatches at the module attribute where load_raw_data looks it up
  </acceptance_criteria>
  <verify>
    <automated>venv/python.exe -m pytest tests/test_ingestion.py -x -q</automated>
    <fails_when>exit code is non-zero, or output contains FAILED or ERROR, or fewer than 4 tests are collected</fails_when>
  </verify>
  <done>Quirk normalization, summarize shape, and config-driven path resolution are pinned by tests using synthetic data only.</done>
</task>

<task type="auto">
  <name>save_outputs() tmp_path integration test with real-dir protection</name>
  <files>tests/test_save_outputs.py</files>
  <read_first>
    - src/preprocessing/preprocess.py (save_outputs signature and body: processed_dir from cfg, train/val/test csv names, preprocessing_pipeline.joblib name; fit_transform_split return shapes)
    - src/utils/config.py (load_config lru_cache behavior the redirect must defeat)
    - tests/test_preprocessing.py (40-row synthetic_df fixture pattern to reuse at similar scale)
  </read_first>
  <action>Create tests/test_save_outputs.py: build a synthetic CKD-shaped frame of at least 40 rows, run clean_raw then split_raw then fit_transform_split, then monkeypatch src.preprocessing.preprocess.load_config to return a config dict identical to the real one except data.processed_dir set to str(tmp_path) (call load_config.cache_clear first), call save_outputs(pipeline, splits, ys), and assert train.csv val.csv test.csv plus preprocessing_pipeline.joblib exist under tmp_path, the three CSV row counts sum to the cleaned input row count, the reloaded joblib pipeline transforms X_test without error, and no file under the real data/processed/ directory was created or modified (assert by snapshotting data/processed/ mtimes before the test and comparing after, or by asserting git status shows no data/processed/ change). Reuse the synthetic_df construction idiom from test_preprocessing.py; never read the real raw CSV.</action>
  <acceptance_criteria>
    - All four artifacts exist under tmp_path with row-count conservation holding exactly
    - The reloaded .joblib transforms held-out features without NotFittedError
    - Real data/processed/ is byte-identical before and after the test run
  </acceptance_criteria>
  <verify>
    <automated>venv/python.exe -m pytest tests/test_save_outputs.py -x -q</automated>
    <fails_when>exit code is non-zero, or output contains FAILED or ERROR, or any assertion on artifact existence fails</fails_when>
    <automated>git status --short data/processed/</automated>
    <fails_when>exit code is non-zero, or any output line mentions data/processed/ (meaning the real dir was polluted)</fails_when>
  </verify>
  <done>save_outputs round-trip is proven isolated, reloadable, and row-conserving with the real output dir untouched.</done>
</task>

<task type="auto">
  <name>Validation-gate accept/reject tests plus full-suite green</name>
  <files>tests/test_validation.py</files>
  <read_first>
    - data/validation/validate.py (run_raw_validation signature and result shape from plan 01-01)
    - data/validation/ckd_suite.json (expectation kwargs the bad frames must violate: label domain, target presence, row-count range)
    - .planning/phases/phase-1/01-01-SUMMARY.md (GX 1.x runner outcome this test file must conform to; no fallback variant)
  </read_first>
  <action>Create tests/test_validation.py importing run_raw_validation from data/validation/validate.py via sys.path insert of PROJECT_ROOT / data / validation (data/ has no __init__, so no package import). Cover four cases on synthetic in-memory frames: a good frame with ckd/notckd labels and at least 40 rows returns success True; a frame with an out-of-domain label returns success False; a frame with the classification column dropped returns success False; a 2-row frame returns success False via the row-count range. Assert rejection cases return success False rather than raising. Finish by running the entire suite and confirming the 4 pre-existing tests plus all new tests pass together with no dependency on data/raw/ files (temporarily renaming data/raw is out of scope; instead assert no test reads the real CSV by grepping the three new files for kidney_disease).</action>
  <acceptance_criteria>
    - tests/test_validation.py contains at least 4 test functions (one accept, three reject) and none of them touches the filesystem raw CSV
    - The strings kidney_disease and data/raw appear zero times across the three new test files
    - Full suite output shows zero failed and zero errors
  </acceptance_criteria>
  <verify>
    <automated>venv/python.exe -m pytest tests/ -q</automated>
    <fails_when>exit code is non-zero, or output contains FAILED or ERROR, or total collected is below 12 (4 existing plus at least 8 new)</fails_when>
  </verify>
  <done>Gate accept/reject behavior is pinned, full suite is green, and no new test depends on the real dataset.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| synthetic test frames → pipeline functions | Adversarial-shaped inputs (quirks, bad labels) cross into production code paths |
| test process → data/processed/ on disk | Test writes could cross into real committed-output state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-1-07 | Tampering | save_outputs test writes | high | mitigate | tmp_path redirect plus mtime/git-status proof that real data/processed/ is untouched (Pitfall 5) |
| T-1-08 | Tampering | quirk-test inputs vs production regex | medium | mitigate | Tests call real load_raw_data; forbidden duplication of the ? regex asserted in acceptance criteria (Pitfall 4) |
| T-1-09 | Information Disclosure | real patient-adjacent CSV in tests | low | mitigate | Synthetic frames only; grep assertion that kidney_disease/data/raw appear zero times in new tests |
| T-1-SC | Tampering | pip installs | low | accept | No installs in this plan; all commands via venv/python.exe with the interpreter already resolved by plan 01-01 |
</threat_model>

<verification>
Each new file passes standalone, the full tests/ run is green with at least 12 collected, git status shows no data/processed/ pollution, and no new test references the real CSV.
</verification>

<success_criteria>
FR-1.1 quirk handling, FR-1.2 summarize shape, FR-1.3 through FR-1.5 via the re-run pre-existing tests, FR-1.6 via the isolated save_outputs round-trip, and FR-1.7 via the config-override test are all pinned; NFR-3 holds because every new test is synthetic-data-only.
</success_criteria>

<output>
Create `.planning/phases/phase-1/01-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

New files: tests/test_ingestion.py (quirk normalization, summarize shape, config-override tests), tests/test_save_outputs.py (tmp_path round-trip with real-dir protection), tests/test_validation.py (gate accept plus three reject cases).
New symbols: test functions only; no production symbols added or changed.
</output>
