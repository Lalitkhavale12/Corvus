---
phase: 02-model-training-experiment-tracking
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/training/train.py
  - src/training/train_mlp.py
  - tests/test_training.py
  - tests/test_registry.py
autonomous: true
requirements: [FR-2.1, FR-2.5, FR-3.1, FR-3.2, FR-3.3]

estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "All 9 configs (LR, Decision Tree, Random Forest, Gradient Boosting, XGBoost, LightGBM per D-01 plus MLP Sigmoid/Tanh/ReLU per D-01/D-04) train with library defaults plus fixed seeds and one rationale each (per D-02), no grid search"
    - "MLP runs share the tracer's run shape, differ only in activation (logistic/tanh/relu, never sigmoid), use hidden_layer_sizes (50,) with fixed max_iter and no early stopping (per D-04/D-05)"
    - "Every run registers a model version (per D-06); winner sits in Staging and all superseded versions sit in Archived (per D-06/D-07); Production promotion remains a separate explicit step"
  artifacts:
    - src/training/train.py
    - src/training/train_mlp.py
    - tests/test_training.py
    - tests/test_registry.py
  key_links:
    - "train_mlp.py reuses the run_experiment shape proven by the 02-01 tracer, differing only in the estimator factory"
    - "evaluate.py from 02-01 ranks all 9 runs unchanged (strict max ROC-AUC) — no evaluate.py rewrite needed"
    - "Every log_model call pins serialization_format so the Phase 3 load path round-trips"
---

<objective>
Expansion from the 02-01 tracer (per D-01, D-02, D-04, D-05, D-06, D-07): grow the proven LR slice to the full locked slate of 9 configs and complete the registry lifecycle (every version registered, winner in Staging, superseded in Archived).

Purpose: Deliver the phase goal — 9 seeded runs logged to MLflow under experiment corvus-ckd with the winner registered — while the tracer guarantees the underlying wiring is already proven.
Output: src/training/train.py with 6 classical configs, src/training/train_mlp.py with 3 MLP runs, green training plus registry tests, full suite green.
</objective>

<execution_context>
@C:/Users/Lalit/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Lalit/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-model-training-experiment-tracking/02-CONTEXT.md
@.planning/phases/02-model-training-experiment-tracking/02-RESEARCH.md
@.planning/phases/02-model-training-experiment-tracking/02-PATTERNS.md
@.planning/phases/02-model-training-experiment-tracking/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full classical slate in train.py — DT, RF, GB, XGB, LGBM with seeds and rationales (FR-2.1 classical, FR-2.5)</name>
  <files>src/training/train.py</files>
  <read_first>src/training/train.py (tracer version: load_raw_splits, build_estimator, run_experiment shapes from 02-01); config/config.yaml (mlflow block lines 35-37)</read_first>
  <action>Extend the ESTIMATORS registry in src/training/train.py to all 6 classical configs per D-01 (logistic-regression entry from the tracer stays untouched): DecisionTreeClassifier with random_state=42, RandomForestClassifier with random_state=42, GradientBoostingClassifier with random_state=42, XGBClassifier with random_state=42 (seed kwarg only, never seed=), LGBMClassifier with random_state=42 and verbosity=-1 (never a verbose= kwarg, which does not exist in LightGBM 4.x). Add exactly one documented rationale comment per estimator choice per D-02; no grid search code anywhere. Extend main() to loop all 6 configs through the unchanged run_experiment() helper so each run keeps the tracer's Claim 2 shape (params plus 5 metrics plus pipeline artifact plus model with registered_model_name corvus-ckd and pinned serialization_format cloudpickle in one start_run block). Keep the LR entry byte-identical so the tracer proof still holds.</action>
  <verify>
    <automated>.\venv\python.exe -m pytest tests/test_training.py -q -k "nine_configs or claim2"</automated>
    <fails_when>exit code is nonzero, or output shows any test with FAILED or ERROR status</fails_when>
  </verify>
  <acceptance_criteria>ESTIMATORS holds exactly 6 classical entries each seeded and rationale-commented; test_all_nine_configs_construct covers the 6 classical entries green; a run against the tmp tracking store produces 6 runs each carrying both pipeline and model artifacts; no verbose= kwarg and no seed= kwarg appear in the file; full transformed-CSV names (train.csv, val.csv, test.csv without raw) appear nowhere in the file.</acceptance_criteria>
  <done>All 6 classical configs train through the proven run shape with seeds, rationales, and per-run Claim 2 artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: train_mlp.py — 3 activation runs as a thin variant (FR-2.1 MLP, FR-2.5)</name>
  <files>src/training/train_mlp.py</files>
  <read_first>src/training/train.py (final version after Task 1: copy the load_raw_splits, run_experiment, and MLflow shapes exactly); src/preprocessing/preprocess.py (LINEAGE constants lines 74-75)</read_first>
  <action>Create src/training/train_mlp.py as a thin variant of train.py per D-01, D-04, and D-05: reuse the identical load_raw_splits and run_experiment shapes (same raw-CSV inputs, same train-only pipeline re-fit, same one-run-holds-params-plus-metrics-plus-pipeline-plus-model MLflow block with registered_model_name corvus-ckd and pinned serialization_format cloudpickle); the ONLY difference is the estimator factory with ACTIVATIONS mapping sigmoid to logistic, tanh to tanh, relu to relu (never the invalid literal sigmoid as an sklearn activation value) and three runs MLPClassifier with hidden_layer_sizes (50,), the mapped activation, explicit max_iter 500, and random_state=42, with fixed budget and no early stopping per D-05. Handle ConvergenceWarning deliberately via a warnings filter that logs through the module logger (warning-not-crash per project conventions, never a bare silent ignore). Entry is def main() plus if __name__ main guard invoked as python -m src.training.train_mlp; module-level log equals get_logger().</action>
  <verify>
    <automated>.\venv\python.exe -m pytest tests/test_training.py -q</automated>
    <fails_when>exit code is nonzero, or output shows any test with FAILED or ERROR status</fails_when>
  </verify>
  <acceptance_criteria>src/training/train_mlp.py exists with ACTIVATIONS map holding logistic/tanh/relu and no sigmoid literal passed to MLPClassifier; running `.\venv\python.exe -m src.training.train_mlp` from project root exits 0 and creates exactly 3 runs (one per activation, hidden_layer_sizes (50,) identical across runs); all test_training.py tests pass including the MLP activation and nine-config assertions.</acceptance_criteria>
  <done>Three comparable MLP runs (differing only in activation) logged through the same Claim 2 run shape.</done>
</task>

<task type="auto">
  <name>Task 3: Full 9-run registry lifecycle — every version registered, winner Staging, superseded Archived (FR-3.1, FR-3.2, FR-3.3)</name>
  <files>tests/test_registry.py, src/evaluation/evaluate.py</files>
  <read_first>src/evaluation/evaluate.py (tracer version: select_winner, apply_registry_stages from 02-01); tests/test_registry.py (tracer stubs from 02-01)</read_first>
  <action>Complete the registry lifecycle per D-06 and D-07 without rewriting the tracer's ranking logic: confirm select_winner() still ranks all 9 runs by strict max test roc_auc with no tie-break (no changes unless the 9-run input shape requires it); confirm apply_registry_stages() moves the winner version to Staging with archive_existing_versions True and every other version to Archived with exact stage strings Staging and Archived; assert in tests/test_registry.py that every one of the 9 runs registers a new version (version count grows by 9), the winner version resolves to Staging, all superseded versions resolve to Archived, and no code path transitions anything to Production (Production stays a separate explicit step per D-06). Run the real 9-config training (train.py plus train_mlp.py) followed by evaluate.py against the real mlruns store once, then verify stages via the MLflow client. Keep the stages-deprecated TECH DEBT comment intact.</action>
  <reversibility rating="costly">Stage transitions on the real mlruns store mutate Registry state; reversible only by further transitions, and re-running training appends new versions that cannot be un-registered by file revert.</reversibility>
  <verify>
    <automated>.\venv\python.exe -m pytest tests -q</automated>
    <fails_when>exit code is nonzero, or output shows any test with FAILED or ERROR status, or the passed count drops below the pre-change count</fails_when>
  </verify>
  <acceptance_criteria>Full suite `.\venv\python.exe -m pytest tests -q` is green including all Phase 1 tests; MLflow experiment corvus-ckd holds 9 runs each with 5 test metrics and both artifacts; Registry model corvus-ckd holds 9 versions with the strict-max-ROC-AUC winner in Staging, the other 8 in Archived, and nothing in Production; test_every_run_registers_version, test_stage_transitions, and test_no_production_transition pass.</acceptance_criteria>
  <done>Phase goal delivered: 9 seeded runs tracked, winner registered to Staging, superseded versions Archived, full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo CSVs to training loaders | Only the repo's own data/processed/*_raw.csv files are read by train.py and train_mlp.py; no external or user-supplied input crosses here |
| mlruns/ file store to Registry readers | All 9 model versions are produced and consumed inside this repo only (local file store, no server, no network) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | joblib.load of preprocessing_pipeline.joblib in train.py and train_mlp.py | low | mitigate | Load only from config-resolved data/processed path; never joblib.load third-party files; serialization_format pinned in every log_model call |
| T-02-02 | Information disclosure | MLflow logged artifacts across 9 runs | low | accept | mlruns/ is gitignored local state and never committed; lineage IDs stay in artifacts, never in metrics or tags |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No install step in this phase (venv already provisioned: sklearn 1.9.1, mlflow 3.16.1, xgboost 3.2.0, lightgbm 4.6.0); no new packages, so no legitimacy gate is triggered |
</threat_model>

<verification>
- `.\venv\python.exe -m pytest tests -q` green (Phase 1 plus Phase 2 tests)
- `.\venv\python.exe -m src.training.train` creates 6 classical runs; `.\venv\python.exe -m src.training.train_mlp` creates 3 MLP runs; each of the 9 runs carries params, 5 test metrics, pipeline artifact, and model artifact
- `.\venv\python.exe -m src.evaluation.evaluate` selects the strict-max-ROC-AUC winner, leaves it in Staging, the other 8 in Archived, nothing in Production
- Manual MLflow UI check (per 02-VALIDATION.md): 9 runs with metrics plus 2 artifacts each under experiment corvus-ckd; Models tab shows versioned corvus-ckd with winner in Staging
</verification>

<success_criteria>
- 9 configs trained with defaults plus fixed seeds and one rationale each; no grid search code exists
- MLP runs differ only in activation with hidden_layer_sizes (50,), fixed max_iter, seeded, no early stopping
- Every run registered a version; winner in Staging; superseded in Archived; Production untouched; full suite green
</success_criteria>

## Artifacts this phase produces (plan 02 share)

- src/training/train.py — ESTIMATORS extended to 6 classical configs (logistic-regression, decision-tree, random-forest, gradient-boosting, xgboost, lightgbm), main() loops all 6
- src/training/train_mlp.py — ACTIVATIONS map (sigmoid to logistic, tanh, relu), 3 MLP runs, main()
- tests/test_training.py — extended assertions: all 9 configs construct, MLP activations valid, claim2 artifact presence for all runs
- tests/test_registry.py — extended assertions: 9 versions registered, winner Staging, 8 superseded Archived, no Production transition

<output>
Create `.planning/phases/02-model-training-experiment-tracking/02-02-SUMMARY.md` when done
</output>
