---
phase: 02-model-training-experiment-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_training.py
  - tests/test_registry.py
  - src/training/train.py
  - src/evaluation/evaluate.py
autonomous: true
requirements: [FR-2.1, FR-2.2, FR-2.3, FR-2.4]

estimate:
  tokens: 55000
  raw_tokens: 27500
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "One model (logistic regression) trains end-to-end from train_raw.csv through the pipeline and logs params, 5 metrics, pipeline artifact, and model in a single MLflow run (per D-01 slice, D-02 seeds, Claim 2 training-input contract)"
    - "evaluate.py ranks runs by strict max test ROC-AUC with no tie-break and moves the winner to Staging (per D-03, D-06)"
    - "Full pytest suite stays green; Wave 0 stubs exist first and are red before implementation"
  artifacts:
    - tests/conftest.py
    - tests/test_training.py
    - tests/test_registry.py
    - src/training/train.py
    - src/evaluation/evaluate.py
  key_links:
    - "train.py reads data/processed/*_raw.csv (never transformed CSVs) and re-fits the pipeline on train-raw only"
    - "MLflow tracking URI resolved against PROJECT_ROOT (never raw relative mlruns)"
    - "ROC-AUC computed from predict_proba[:, 1], never from labels"
---

<objective>
Tracer slice for Phase 2 (per D-01, D-02, D-03, D-06 and the Claim 2 training-input contract): prove the full train-to-MLflow-to-evaluate-to-registry path with ONE production-quality model (logistic regression) before the remaining 8 configs are added in plan 02-02.

Purpose: Catch architectural dead-ends (MLflow file-store shape, Claim 2 same-run artifact contract, registry stage API on installed mlflow 3.16.1) on the first commit instead of after all 9 configs exist.
Output: Working LR end-to-end path plus Wave 0 test stubs that later plans turn green for all 9 configs.
</objective>

<execution_context>
@C:/Users/Lalit/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Lalit/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-model-training-experiment-tracking/02-CONTEXT.md
@.planning/phases/02-model-training-experiment-tracking/02-RESEARCH.md
@.planning/phases/02-model-training-experiment-tracking/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 test stubs (conftest + test_training + test_registry)</name>
  <files>tests/conftest.py, tests/test_training.py, tests/test_registry.py</files>
  <read_first>tests/test_save_outputs.py (tmp_path isolation + real-dir snapshot pattern, lines 52-68 and 102-103); tests/test_preprocessing.py (40-row synthetic CKD fixture, lines 21-34; config-truth assertion, lines 37-46); src/preprocessing/preprocess.py (LINEAGE_ROW_COL and LINEAGE_ID_COL constants, lines 74-75)</read_first>
  <action>Create tests/conftest.py with a shared 40-row synthetic CKD-shaped fixture (numeric cols as float(i mod 5), categorical cols alternating yes/no, classification alternating ckd/notckd with one None injected per type) plus a tmp_mlflow_store fixture pointing the tracking URI at tmp_path (never the real mlruns directory), following the test_save_outputs.py monkeypatched-load_config isolation shape. Create tests/test_training.py stubs asserting all-nine-configs-construct (per D-01), lineage-excluded feature count equals 24 (per Claim 2 training-input contract), loader never references non-raw CSV names, and run-logs-pipeline-and-model claim2 test. Create tests/test_registry.py stubs asserting strict-max ROC-AUC winner selection (per D-03), winner-to-Staging transition, superseded-to-Archived transitions with exact stage strings, and no Production transition (per D-06/D-07). Stubs must fail (assert False with a STUB message or reference not-yet-existing symbols) so the tracer starts red.</action>
  <verify>
    <automated>.\venv\python.exe -m pytest tests/test_training.py tests/test_registry.py -q</automated>
    <fails_when>exit code is 0 (all tests pass) — Wave 0 stubs must be red before implementation exists</fails_when>
  </verify>
  <acceptance_criteria>tests/conftest.py, tests/test_training.py, tests/test_registry.py exist; test modules collect without import errors; every stub test fails (red), proving the tests execute and await implementation; real data/ and real mlruns/ untouched (no new files under either).</acceptance_criteria>
  <done>Wave 0 stubs collected and red; shared synthetic fixture and tmp tracking-URI fixture available to later tasks.</done>
</task>

<task type="tracer">
  <name>Task 2: End-to-end logistic-regression path — raw CSVs through pipeline to one MLflow run (FR-2.1 slice, FR-2.2, FR-2.3)</name>
  <files>src/training/train.py</files>
  <read_first>src/preprocessing/preprocess.py (config-driven main lines 284-302, immutable-input pattern lines 100-123, build_pipeline definition lines 172-206, LINEAGE constants lines 74-75); src/utils/config.py (load_config deepcopy semantics and PROJECT_ROOT, full file); config/config.yaml (data.numeric_cols and categorical_cols lines 10-12, split block lines 19-23, mlflow tracking_uri and experiment_name lines 35-37)</read_first>
  <action>Create src/training/train.py implementing the uniform per-run shape that plan 02-02 will reuse for all 9 configs (per D-01): load_raw_splits() reads data/processed train_raw.csv, val_raw.csv, test_raw.csv via load_config processed_dir, drops LINEAGE_ROW_COL and LINEAGE_ID_COL imported from src.preprocessing.preprocess (never re-typed) plus the configured target column, and binary-encodes the target via config data.positive_label; build_estimator(name) factory with an ESTIMATORS registry seeded random_state=42 containing the logistic-regression entry LogisticRegression with random_state=42 plus a one-line rationale comment per D-02; run_experiment() re-fits a fresh pipeline from build_pipeline on train-raw only, transforms val and test, fits the estimator, computes all 5 metrics on test with roc_auc from predict_proba test rows column 1 (never labels), and inside one mlflow.start_run block logs params, the 5 metrics, the pipeline file via log_artifact under artifact path preprocessing, and the model via mlflow.sklearn.log_model with registered_model_name corvus-ckd and pinned serialization_format cloudpickle; tracking URI set as str(PROJECT_ROOT slash cfg mlflow tracking_uri) and experiment from cfg mlflow experiment_name; def main() plus if __name__ main guard invoked as python -m src.training.train; module-level log equals get_logger(), immutable DataFrame copies on entry, log.warning not crash on unexpected columns. Never reference transformed train.csv, val.csv, or test.csv in any code path.</action>
  <verify>
    <automated>.\venv\python.exe -m pytest tests/test_training.py -q</automated>
    <fails_when>exit code is nonzero, or output shows any test with FAILED or ERROR status</fails_when>
  </verify>
  <acceptance_criteria>src/training/train.py exists with load_raw_splits, build_estimator, run_experiment, ESTIMATORS, and main; running `.\venv\python.exe -m src.training.train` from project root exits 0 and creates exactly one run named logistic-regression under experiment corvus-ckd carrying 5 test metrics (accuracy, precision, recall, f1, roc_auc) and both the preprocessing artifact and the model artifact in that same run; trained feature count is 24 with no lineage column in X; test_run_logs_pipeline_and_model and lineage tests pass.</acceptance_criteria>
  <done>The single LR happy path works end-to-end (raw CSVs to one Claim 2-compliant MLflow run) and is committed.</done>
</task>

<task type="auto">
  <name>Task 3: evaluate.py — proba-based metrics, strict ROC-AUC ranking, Staging transition (FR-2.4, FR-2.5 slice)</name>
  <files>src/evaluation/evaluate.py</files>
  <read_first>src/validation/validate.py (fail-closed gate plus int-exit main pattern, lines 86-140); src/utils/config.py (load_config and PROJECT_ROOT); config/config.yaml (mlflow block lines 35-37)</read_first>
  <action>Create src/evaluation/evaluate.py with compute_metrics() returning accuracy, precision, recall, f1 (all with zero_division=0), and roc_auc from predict_proba column 1 with a comment citing Pitfall 8; select_winner() taking run records and returning the strict max by test roc_auc with no tie-break logic per D-03; apply_registry_stages() transitioning the winner version to Staging with archive_existing_versions True and every superseded version to Archived via MlflowClient transition_model_version_stage, and never transitioning anything to Production per D-06; run_evaluation() reading logged test metrics via MlflowClient search_runs on experiment corvus-ckd and returning a result object carrying success bool; def main() returning int with sys.exit(main()) gate following the validate.py shape. Use the single registered-model name corvus-ckd chosen per RESEARCH open question 1. Record the MLflow 3.x stages-deprecated-but-functional status plus the alias-migration follow-up as a TECH DEBT comment at the top of apply_registry_stages per Pitfall 2.</action>
  <reversibility rating="costly">Stage transitions mutate Registry state in mlruns/; reversible only by issuing further transitions, never by file revert alone.</reversibility>
  <verify>
    <automated>.\venv\python.exe -m pytest tests/test_registry.py -q</automated>
    <fails_when>exit code is nonzero, or output shows any test with FAILED or ERROR status</fails_when>
  </verify>
  <acceptance_criteria>src/evaluation/evaluate.py exists with compute_metrics, select_winner, apply_registry_stages, run_evaluation, main; running `.\venv\python.exe -m src.evaluation.evaluate` after Task 2 exits 0, prints the winner line naming logistic-regression, leaves the winner version in Staging, transitions nothing to Production; all test_registry.py tests pass including exact stage-string and no-Production assertions.</acceptance_criteria>
  <done>Strict ROC-AUC ranking and winner-to-Staging registry lifecycle proven end-to-end on the tracer run.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo CSVs to training loader | Only the repo's own data/processed/*_raw.csv files are read; no external or user-supplied input crosses here |
| mlruns/ file store to loader | Pipeline and model artifacts are produced and consumed inside this repo only (local file store, no server, no network) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | joblib.load of preprocessing_pipeline.joblib in train.py | low | mitigate | Load only from config-resolved data/processed path; never joblib.load third-party files; pin serialization_format explicitly in log_model calls |
| T-02-02 | Information disclosure | MLflow logged artifacts | low | accept | mlruns/ is gitignored local state and never committed; lineage IDs stay in artifacts, never in metrics or tags |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No install step in this phase (venv already provisioned: sklearn 1.9.1, mlflow 3.16.1, xgboost 3.2.0, lightgbm 4.6.0); no new packages, so no legitimacy gate is triggered |
</threat_model>

<verification>
- `.\venv\python.exe -m pytest tests -q` is green (Phase 1 tests unbroken, new stubs either red-then-green per task order)
- `.\venv\python.exe -m src.training.train` from project root creates exactly one run under mlruns/ in experiment corvus-ckd with 5 test metrics plus both preprocessing and model artifacts
- `.\venv\python.exe -m src.evaluation.evaluate` prints the winner line and leaves the winner version in Staging with no Production transition
</verification>

<success_criteria>
- LR trains end-to-end from train_raw.csv through a train-only re-fit pipeline into one MLflow run carrying both Claim 2 artifacts
- Winner ranking is strict max test ROC-AUC computed from probabilities
- Registry holds the LR version with winner in Staging; full suite green
</success_criteria>

## Artifacts this phase produces (plan 01 share)

- tests/conftest.py — synthetic_ckd_df fixture (40 rows), tmp_mlflow_store fixture (tracking URI under tmp_path)
- tests/test_training.py — test_all_nine_configs_construct, test_lineage_excluded_feature_count_24, test_loader_reads_raw_only, test_run_logs_pipeline_and_model
- tests/test_registry.py — test_winner_is_strict_max_roc_auc, test_stage_transitions, test_every_run_registers_version, test_no_production_transition
- src/training/train.py — load_raw_splits(), build_estimator(name), run_experiment(), ESTIMATORS (LR entry), main()
- src/evaluation/evaluate.py — compute_metrics(), select_winner(), apply_registry_stages(), run_evaluation(), main()

<output>
Create `.planning/phases/02-model-training-experiment-tracking/02-01-SUMMARY.md` when done
</output>

