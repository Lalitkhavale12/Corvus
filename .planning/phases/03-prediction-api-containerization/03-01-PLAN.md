---
phase: 03-prediction-api-containerization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/promote_production.py
  - config/config.yaml
  - requirements.txt
  - tests/conftest.py
  - tests/test_api.py
autonomous: false
requirements: [FR-4.4, FR-4.3]
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "corvus-ckd v9 serves from the Production stage (D-01 first act)"
    - "API config carries a pinned serving version readable at startup (D-02)"
    - "Wave 0 API stubs exist and are red until the tracer implements the endpoints"
  artifacts:
    - scripts/promote_production.py
    - tests/test_api.py
    - config/config.yaml (api.* + db.* sections)
  key_links:
    - "scripts/promote_production.py -> MLflow Registry corvus-ckd v9 Production stage"
    - "config/config.yaml api.pinned_version -> 03-02 lifespan pin (D-02)"
    - "tests/test_api.py stubs -> 03-02 green run"
---

<objective>
Foundation for Phase 3 per D-01 and D-02: promote corvus-ckd v9 Staging to Production as the first act, install and record the three serving dependencies, extend config with api.* and db.* sections, and land red Wave 0 API test stubs that 03-02 turns green.

Purpose: Nothing downstream can serve or be tested until Production is populated, pinned-version config exists, and failing tests define the contract.
Output: Promotion script plus run proof, updated config and requirements, Wave 0 fixtures and stubs.
</objective>

<execution_context>
@C:/Users/Lalit/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Lalit/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-prediction-api-containerization/03-CONTEXT.md
@.planning/phases/03-prediction-api-containerization/03-RESEARCH.md
@.planning/phases/03-prediction-api-containerization/03-PATTERNS.md
@.planning/phases/03-prediction-api-containerization/03-VALIDATION.md
@src/evaluation/evaluate.py
@src/utils/config.py
@tests/conftest.py
@tests/test_registry.py
@config/config.yaml
@requirements.txt
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Approve the three serving-dependency installs</name>
  <read_first>03-RESEARCH.md Package Legitimacy Audit; requirements.txt</read_first>
  <action>Present psycopg2-binary, streamlit>=1.32, and python-multipart for human verification per D-all install policy: each is SUS on seam-metadata gaps only (too-new publication seam, unknown download counts), each is the canonical package for its job (SQLAlchemy default Postgres DBAPI; STATE.md D4 Streamlit decision; FastAPI-mandated UploadFile parser), no postinstall scripts reported. Human checks each name at pypi.org/project/<name> and its source repo (psycopg.org, streamlit.io, github.com/Kludex/python-multipart), then approves. No install runs inside this task; installation is Task 2 after approval.</action>
  <verify>Human approval recorded in session before Task 2 runs.</verify>
  <done>Explicit human approval for all three package names exists; Task 2 is unblocked.</done>
  <acceptance_criteria>Approval names exactly psycopg2-binary, streamlit>=1.32, python-multipart; no substitute package approved; approval precedes any pip install.</acceptance_criteria>
</task>

<task type="auto">
  <name>Install deps, extend config, promote v9 to Production</name>
  <files>requirements.txt, config/config.yaml, scripts/promote_production.py</files>
  <read_first>src/evaluation/evaluate.py lines on apply_registry_stages and main; src/utils/config.py load_config; config/config.yaml full file; requirements.txt full file</read_first>
  <precondition>Local mlruns/ registry holds corvus-ckd v9 in Staging with v1-v8 Archived (Phase 2 canonical pass output).</precondition>
  <reversibility rating="costly">Registry stage transition is audited and reversible only via an explicit second transition; promotion archives nothing new since Production is empty.</reversibility>
  <action>Run the Task 1 approved install command .\venv\python.exe -m pip install psycopg2-binary streamlit>=1.32 python-multipart and append the three pins to requirements.txt while fixing the mislabeled "# --- API (Phase 4) ---" header to Phase 3 per D-01 through D-08 scope. Append api section (model_name corvus-ckd, pinned_version "9", tracking_uri honoring MLFLOW_TRACKING_URI env with mlruns fallback) and db section (database_url empty default, read from DATABASE_URL env) to config/config.yaml in the existing flat key style via load_config, no second config mechanism. Create scripts/promote_production.py mirroring evaluate.py apply_registry_stages call shape with stage Production and archive_existing_versions True for version 9 only, module constant REGISTERED_MODEL_NAME corvus-ckd, def main() -> int printing PROMOTED corvus-ckd version=9 stage=Production on success and PROMOTION ERROR on exception, sys.exit(main()) entry point, module-level log = get_logger(). Run it via .\venv\python.exe scripts/promote_production.py and confirm v9 Production.</action>
  <verify>
    <automated>.\venv\python.exe scripts/promote_production.py</automated>
    <fails_when>Exit code is not 0, or stdout lacks the line PROMOTED corvus-ckd version=9 stage=Production.</fails_when>
    <automated>.\venv\python.exe -m pytest tests -q --ignore=tests/test_api.py</automated>
    <fails_when>Exit code is not 0, or the output contains the string FAILED.</fails_when>
  </verify>
  <done>requirements.txt pins all three packages under a Phase 3 header; config.yaml has api and db sections; script exit 0 with the PROMOTED line; existing suite green excluding the not-yet-created test_api.py.</done>
  <acceptance_criteria>pip list shows psycopg2-binary, streamlit, python-multipart; MlflowClient get_model_version corvus-ckd 9 reports stage Production; load_config returns api.pinned_version 9 and model_name corvus-ckd; suite reports 26 passed, 0 failed.</acceptance_criteria>
</task>

<task type="auto">
  <name>Land Wave 0 fixtures and red API stubs</name>
  <files>tests/conftest.py, tests/test_api.py</files>
  <read_first>tests/conftest.py full file; tests/test_registry.py mock-client and negative-assertion patterns; 03-RESEARCH.md 24-field schema table; data/processed/test_raw.csv header row only</read_first>
  <action>Add synthetic_ckd_request dict fixture to tests/conftest.py built from get_column_lists (14 numeric keys with rc None, 10 categorical keys with valid RESEARCH-table literals rbc normal, pc normal, pcc notpresent, ba notpresent, htn no, dm no, cad no, appet good, pe no, ane no) plus synthetic_batch_csv fixture returning CSV text of 3 valid rows with the 24-column header. Create tests/test_api.py importing fastapi.testclient.TestClient and api.app app, using with TestClient(app) as client so lifespan runs, with eight stub tests asserting POST /predict returns model_version 9 with prediction and probability keys, missing-field 422, bad-Literal 422, null-numeric 200 via imputer path, GET /health 200 with model_version, GET /model_info pinned version 9 with pinned-at-startup resolution note, batch all-or-nothing 422 naming row 2, and logged row carrying model_version via mocked session. Stubs stay red in this plan by design; 03-02 implements the app. No real mlruns, no real Postgres; tmp_mlflow_store plus mocked SQLAlchemy session only.</action>
  <verify>
    <automated>.\venv\python.exe -m pytest tests/test_api.py -q</automated>
    <fails_when>Exit code is 0. Wave 0 stubs must be red until 03-02 implements api/app.py; a green run here means the stubs assert nothing.</fails_when>
  </verify>
  <done>tests/test_api.py exists with at least 8 test functions, collection fails only on missing api.app, conftest fixtures import cleanly.</done>
  <acceptance_criteria>Exactly two files touched; pytest output shows collection error naming api.app or at least 1 failed; grep of tests/test_api.py for model_version returns at least 3 hits; no test opens a live server or real database connection.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pypi.org -> venv | Three new packages enter the supply chain in this plan |
| scripts/promote_production.py -> MLflow file store | Stage mutation against local mlruns registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | requirements.txt new pins | medium | mitigate | Task 1 blocking-human checkpoint verifies each name at pypi.org plus source repo before install; no version ranges wider than documented |
| T-03-02 | Repudiation | scripts/promote_production.py | low | mitigate | Script prints PROMOTED line with version and stage; MLflow registry audit trail records the transition |
| T-03-SC | Tampering | pip installs psycopg2-binary, streamlit, python-multipart | high | mitigate | Blocking human checkpoint per emerging package-install policy; SUS verdicts are seam-metadata gaps, install proceeds only on explicit approval; no postinstall scripts reported |
</threat_model>

<verification>
Promotion proof plus existing suite green excluding test_api.py; Wave 0 stubs present and red.
</verification>

<success_criteria>
v9 Production confirmed; config api/db sections load via load_config; three packages installed and pinned; test_api.py red with 8-plus stubs ready for 03-02.
</success_criteria>

## Artifacts this phase produces

Full phase symbol table (this plan delivers the starred rows):

| Symbol | Kind | File | Status in this plan |
|--------|------|------|---------------------|
| promote_production.main -> int | function | scripts/promote_production.py | * created, run, v9 Production |
| REGISTERED_MODEL_NAME = corvus-ckd | constant | scripts/promote_production.py | * created |
| api.pinned_version / api.model_name / db.database_url | config keys | config/config.yaml | * created |
| psycopg2-binary, streamlit>=1.32, python-multipart | dependency pins | requirements.txt | * created |
| synthetic_ckd_request, synthetic_batch_csv | fixtures | tests/conftest.py | * created |
| tests/test_api.py 8 stubs | tests | tests/test_api.py | * created, red |
| CKDRequest, PredictionResponse | classes | api/schemas.py | 03-02 |
| resolve_pinned_version, load_serving_artifacts | functions | api/model_loader.py | 03-02 |
| PredictionLog, get_engine | class, function | api/db.py | 03-02 |
| FastAPI app with /predict /health /model_info | app + routes | api/app.py | 03-02 |
| POST /batch_predict all-or-nothing | route | api/app.py | 03-03 |
| Dockerfile, docker-compose.yml | container config | docker/ | 03-04 |
| Streamlit app | UI | frontend/app.py | 03-04 |

<output>
Create `.planning/phases/03-prediction-api-containerization/03-01-SUMMARY.md` when done
</output>
