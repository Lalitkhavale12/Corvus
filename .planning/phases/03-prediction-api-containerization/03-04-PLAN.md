---
phase: 03-prediction-api-containerization
plan: 04
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - docker/Dockerfile
  - docker/docker-compose.yml
  - frontend/app.py
autonomous: true
requirements: [FR-5.1, FR-5.2, FR-4.1]
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 27000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "docker compose config validates the api plus mlflow plus postgres stack (FR-5.1)"
    - "Compose Postgres carries prediction logging over DATABASE_URL with no SQLite (D-03, FR-5.2)"
    - "Streamlit UI predicts from a 24-field form and a CSV upload against the API (FR-4.1)"
  artifacts:
    - docker/Dockerfile
    - docker/docker-compose.yml
    - frontend/app.py
  key_links:
    - "compose api service -> DATABASE_URL postgres plus MLFLOW_TRACKING_URI mlflow service"
    - "mlflow service volume-mount -> host mlruns file store (zero-migration OQ2 decision)"
    - "frontend/app.py API_URL -> /predict plus /batch_predict (thin client, zero model logic)"
---

<objective>
Deployment expansion per D-03: containerize the 03-02 API with a health-gated Compose stack (FastAPI plus MLflow plus PostgreSQL) and ship the thin Streamlit frontend that posts to /predict and /batch_predict.

Purpose: Turns the proven local tracer into a runnable demo stack with versioned logging intact and a human-usable UI.
Output: Dockerfile, validated compose file, Streamlit app, built API image.
</objective>

<execution_context>
@C:/Users/Lalit/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Lalit/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-prediction-api-containerization/03-CONTEXT.md
@.planning/phases/03-prediction-api-containerization/03-RESEARCH.md
@.planning/phases/03-prediction-api-containerization/03-PATTERNS.md
@.planning/phases/03-prediction-api-containerization/03-VALIDATION.md
@.planning/phases/03-prediction-api-containerization/03-02-SUMMARY.md
@api/app.py
@requirements.txt
@config/config.yaml
</context>

<tasks>

<task type="auto">
  <name>Write Dockerfile and health-gated Compose stack</name>
  <files>docker/Dockerfile, docker/docker-compose.yml</files>
  <read_first>requirements.txt pinned packages; api/app.py lifespan env expectations; config/config.yaml mlflow section; 03-RESEARCH.md compose plus Dockerfile examples and Open Question 2 recommendation</read_first>
  <action>Write docker/Dockerfile FROM python:3.11-slim matching the 3.11.0 venv, copying requirements.txt first for layer caching then api plus src plus config trees, setting ENV MLFLOW_ALLOW_FILE_STORE true for the container process, exposing 8000, CMD uvicorn api.app:app with host 0.0.0.0 port 8000. Write docker/docker-compose.yml with db on postgres:16 with corvus credentials, pgdata volume, and pg_isready CMD-SHELL healthcheck with service_healthy gating; api built from context parent with dockerfile docker/Dockerfile, ports 8000:8000, environment DATABASE_URL postgresql+psycopg2://corvus:corvus@db:5432/corvus and MLFLOW_TRACKING_URI http://mlflow:5000, depends_on db healthy; mlflow service reusing the api image build running mlflow server with backend-store-uri file:///mlruns and default-artifact-root file:///mlruns on port 5000 with host ./mlruns mounted at /mlruns, recording the Open Question 2 volume-mount decision (zero migration, same store the D-01 promotion wrote). Dev-only compose credentials stay out of git-ignored .env handling scope; never bake production secrets.</action>
  <verify>
    <automated>docker compose -f docker/docker-compose.yml config</automated>
    <fails_when>Exit code is not 0, or the rendered output contains the string error.</fails_when>
  </verify>
  <done>Compose config renders three services with health-gated startup and correct service URLs.</done>
  <acceptance_criteria>Rendered config contains services db, api, mlflow; api environment contains DATABASE_URL with @db:5432 and MLFLOW_TRACKING_URI http://mlflow:5000; db healthcheck test contains pg_isready; Dockerfile first line is FROM python:3.11-slim and CMD contains uvicorn api.app:app.</acceptance_criteria>
</task>

<task type="auto">
  <name>Ship the thin Streamlit frontend</name>
  <files>frontend/app.py</files>
  <read_first>api/schemas.py CKDRequest field names and vocabularies; api/app.py response shapes for predict and batch; 03-RESEARCH.md Assumption A2 thin-client shape</read_first>
  <action>Create frontend/app.py reading API_URL env defaulting to http://localhost:8000, with a st.form collecting all 24 fields (numeric text inputs allowing blanks for the None imputer path per D-05, categorical selectboxes limited to the RESEARCH vocabularies) posting JSON to POST /predict via httpx and displaying prediction, probability, and model_version plus the 422 detail on rejection, and a st.file_uploader CSV path posting multipart files to POST /batch_predict and rendering returned predictions with st.dataframe. Zero model logic, zero range validation, zero Pydantic duplication in the client; server owns all 422 decisions per the responsibility map. Handle connection failure with a clear message naming API_URL.</action>
  <verify>
    <automated>.\venv\python.exe -m py_compile frontend/app.py</automated>
    <fails_when>Exit code is not 0, or stderr names a syntax error in frontend/app.py.</fails_when>
    <automated>.\venv\python.exe -m pytest tests -q</automated>
    <fails_when>Exit code is not 0, or the output contains the string FAILED.</fails_when>
  </verify>
  <done>Frontend compiles, full suite stays green, UI covers single form plus CSV upload against the documented response shapes.</done>
  <acceptance_criteria>File references API_URL env with localhost:8000 default, posts to /predict and /batch_predict paths, displays the string model_version, imports no sklearn, mlflow, or sqlalchemy module.</acceptance_criteria>
</task>

<task type="auto">
  <name>Build the API image and smoke the stack</name>
  <files>docker/docker-compose.yml</files>
  <read_first>docker/Dockerfile; docker/docker-compose.yml rendered config; api/app.py /health and /model_info shapes</read_first>
  <action>Run docker compose build for the api service from the project root, fix Dockerfile or compose wiring on build errors without touching api source semantics, then bring the stack up and prove GET /health returns 200 with model_version 9 and GET /model_info reports version 9, plus one POST /predict round trip returning model_version, recording results in the plan SUMMARY. Tear the stack down afterward so no daemon state leaks into later phases. Manual browser check of streamlit run frontend/app.py against the live stack is recorded as a manual-only verification per 03-VALIDATION.md.</action>
  <verify>
    <automated>docker compose -f docker/docker-compose.yml build api</automated>
    <fails_when>Exit code is not 0, or the output contains the string ERROR.</fails_when>
  </verify>
  <done>API image builds; live smoke of health, model_info, and one prediction recorded; stack torn down.</done>
  <acceptance_criteria>Build completes with exit 0; SUMMARY records /health 200 with model_version 9, /model_info version 9, and one /predict response carrying model_version; docker compose down run leaves no Corvus containers.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet -> published ports 8000/5000 | Demo stack binds locally; no auth in scope |
| Compose env -> containers | Dev credentials flow over DATABASE_URL inside the compose network |
| Streamlit file upload -> API | User CSV crosses into /batch_predict parsing already hardened in 03-03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-12 | Information disclosure | docker-compose.yml credentials | medium | accept | Dev-only corvus/corvus credentials for local demo; .env gitignored so real secrets never commit; rationale recorded here per ASVS L1 |
| T-03-13 | Tampering | Docker image contents | low | mitigate | python:3.11-slim base matching training Python, requirements copied from the pinned file extended in 03-01; no new packages introduced in this plan |
| T-03-14 | Denial of service | api startup race | low | mitigate | pg_isready healthcheck plus depends_on service_healthy; lifespan create_tables retries before failing loud |
| T-03-15 | Elevation of privilege | mlflow service port 5000 | low | accept | Local demo binds only; no auth layer in Phase 3 scope; Phase 4 can restrict exposure |
</threat_model>

<verification>
Compose config renders, image builds, live endpoints report pinned version 9, suite green.
</verification>

<success_criteria>
docker compose config exit 0; api image builds; live /health plus /model_info plus /predict prove version 9 serving; Streamlit compiles and exercises both prediction paths.
</success_criteria>

## Artifacts this phase produces

Plan-scoped symbols (this plan):

| Symbol | Kind | File |
|--------|------|------|
| API image (uvicorn api.app:app 0.0.0.0:8000) | container | docker/Dockerfile |
| db, api, mlflow services | orchestration | docker/docker-compose.yml |
| Streamlit app (form plus CSV upload over API_URL) | UI | frontend/app.py |

<output>
Create `.planning/phases/03-prediction-api-containerization/03-04-SUMMARY.md` when done
</output>
