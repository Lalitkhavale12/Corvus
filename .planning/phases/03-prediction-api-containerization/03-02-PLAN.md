---
phase: 03-prediction-api-containerization
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - api/__init__.py
  - api/schemas.py
  - api/model_loader.py
  - api/db.py
  - api/app.py
  - tests/test_api.py
autonomous: true
requirements: [FR-4.1, FR-4.2, FR-4.3, FR-4.4]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 36000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "POST /predict with 24 valid fields returns prediction plus probability plus model_version 9 (D-05, D-04, FR-4.3)"
    - "Bad or missing fields get a clear 422 before inference (D-06, FR-4.2)"
    - "GET /health and GET /model_info report the startup-pinned version (D-02, FR-4.4 with recorded deviation)"
    - "Wave 0 stubs from 03-01 are green without a live server or real Postgres"
  artifacts:
    - api/schemas.py
    - api/model_loader.py
    - api/db.py
    - api/app.py
  key_links:
    - "lifespan pin -> models:/corvus-ckd/9 load plus pipeline artifact from version run_id"
    - "CKDRequest -> pipeline.transform -> predict/proba -> PredictionResponse with model_version"
    - "predict handler -> PredictionLog row carrying the same pinned model_version"
---

<objective>
Tracer slice per D-02, D-04, D-05, D-06: wire one production-quality path from the 03-01 Production registry through startup-pinned serving to live /predict plus /health plus /model_info, stamping model_version 9 on every response and log row, and turn the 03-01 stubs green.

Purpose: Proves the serving architecture end to end on early-context tokens before batch, Docker, and frontend expand from it.
Output: api package with schemas, loader, DB layer, FastAPI app; green tests/test_api.py.
</objective>

<execution_context>
@C:/Users/Lalit/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Lalit/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-prediction-api-containerization/03-CONTEXT.md
@.planning/phases/03-prediction-api-containerization/03-RESEARCH.md
@.planning/phases/03-prediction-api-containerization/03-PATTERNS.md
@.planning/phases/03-prediction-api-containerization/03-VALIDATION.md
@.planning/phases/03-prediction-api-containerization/03-01-SUMMARY.md
@src/preprocessing/preprocess.py
@src/evaluation/evaluate.py
@src/training/train.py
@src/utils/config.py
@src/utils/logger.py
@src/validation/validate.py
@tests/test_api.py
@tests/conftest.py
@config/config.yaml
</context>

<tasks>

<task type="auto">
  <name>Build 24-field schemas and the pinned model loader</name>
  <files>api/__init__.py, api/schemas.py, api/model_loader.py</files>
  <read_first>src/preprocessing/preprocess.py get_column_lists and lineage constants; src/evaluation/evaluate.py tracking-URI plus run_id linkage lines; src/training/train.py artifact-path lines; 03-RESEARCH.md 24-field schema table and lifespan example; config/config.yaml api section</read_first>
  <action>Create api/schemas.py with CKDRequest carrying all 24 config-derived fields as required attributes per D-05 (14 numerics age bp sg al su bgr bu sc sod pot hemo pcv wc rc typed float or None so blanks flow to imputers, 10 categoricals rbc pc pcc ba htn dm cad appet pe ane as Literal vocabularies from the RESEARCH table, no ge or le bounds per raw extremes bp 180 and sc 76), PredictionResponse with prediction int plus probability float plus model_version str for Claim 4 per D-04, and HealthResponse plus ModelInfoResponse shapes. Create api/model_loader.py with REGISTERED_MODEL_NAME corvus-ckd, MLFLOW_ALLOW_FILE_STORE setdefault at import, resolve_pinned_version reading api.pinned_version with MODEL_VERSION env override, and load_serving_artifacts loading models:/corvus-ckd/<version> via mlflow.sklearn.load_model plus downloading artifact path preprocessing/preprocessing_pipeline.joblib from the version run_id via get_model_version, then asserting pipeline.transform of one 24-column row has width equal to estimator n_features_in_ before returning model, pipeline, run_id, version. Every new module opens with log = get_logger(). Record the FR-4.4 deviation in a module docstring: pinned-at-startup per D-02 satisfies resolve-from-Registry at boot, swaps need restarts.</action>
  <verify>
    <automated>.\venv\python.exe -m py_compile api/schemas.py api/model_loader.py api/__init__.py</automated>
    <fails_when>Exit code is not 0, or stderr names a syntax error in any of the three files.</fails_when>
    <automated>.\venv\python.exe -m pytest tests -q --ignore=tests/test_api.py</automated>
    <fails_when>Exit code is not 0, or the output contains the string FAILED.</fails_when>
  </verify>
  <done>Schemas import with 24 required fields; loader resolves version 9 against the real registry in a manual probe; existing suite stays green.</done>
  <acceptance_criteria>CKDRequest.model_fields has exactly 24 keys matching config numeric plus categorical lists; CKDRequest with rc None validates while a dropped age key raises ValidationError; resolve_pinned_version returns the string 9; docstring contains the sentence swaps need restarts.</acceptance_criteria>
</task>

<task type="auto">
  <name>Build DB layer and FastAPI app with predict, health, model_info</name>
  <files>api/db.py, api/app.py</files>
  <read_first>api/schemas.py; api/model_loader.py; src/validation/validate.py fail-closed and main patterns; src/training/train.py warn-on-extras lines; 03-RESEARCH.md prediction-logging and compose-env examples; config/config.yaml db section</read_first>
  <precondition>config/config.yaml api.pinned_version is 9 and corvus-ckd v9 is in Production (03-01 proof).</precondition>
  <reversibility rating="reversible">New files only; lifespan pin changes take effect on restart with no data migration.</reversibility>
  <action>Create api/db.py with SQLAlchemy 2.0 DeclarativeBase PredictionLog table named predictions holding 24 field columns plus prediction Integer plus probability Float plus model_version String plus created_at DateTime with server default, get_engine reading DATABASE_URL env then config db.database_url, and create_tables with a three-attempt retry. Create api/app.py with lifespan context manager that pins app.state.model_version, loads model plus pipeline plus run_id, creates tables, and fails loud on shape mismatch; POST /predict building a single-row DataFrame in config column order, pipeline.transform then predict plus predict_proba, stamping pinned model_version per D-04 and FR-4.3, writing one PredictionLog row in a per-request Session with a single commit; GET /health returning status ok plus model_version; GET /model_info returning model_name corvus-ckd plus model_version plus run_id plus resolution pinned-at-startup documenting the D-02 deviation from FR-4.4 per-request wording. DATABASE_URL unset selects the fail-closed branch: predict returns 503 with detail prediction log unavailable naming DATABASE_URL, never silent no-log serving and never SQLite per D-03. Unexpected inference exceptions log with type name and return 500 JSON with no traceback; Pydantic failures stay automatic 422 per D-06; never log DATABASE_URL values.</action>
  <verify>
    <automated>.\venv\python.exe -m py_compile api/db.py api/app.py</automated>
    <fails_when>Exit code is not 0, or stderr names a syntax error in api/db.py or api/app.py.</fails_when>
    <automated>.\venv\python.exe -m pytest tests/test_api.py -q -k "predict or health or model_info or version"</automated>
    <fails_when>Exit code is not 0, or the output contains the string FAILED.</fails_when>
  </verify>
  <done>App boots under TestClient with-block, predict returns versioned JSON, 422 and 503 branches behave, log row shape asserted via mocked session.</done>
  <acceptance_criteria>With-block TestClient POST /predict valid body returns 200 with keys prediction, probability, model_version equal to 9; missing age returns 422; rbc ripe returns 422; rc null returns 200; GET /health returns status ok; GET /model_info model_version is 9 and resolution is pinned-at-startup; unset DATABASE_URL POST returns 503.</acceptance_criteria>
</task>

<task type="auto">
  <name>Prove the tracer end to end and hold the full suite green</name>
  <files>tests/test_api.py</files>
  <read_first>tests/test_api.py current stubs; tests/conftest.py synthetic_ckd_request fixture; api/app.py lifespan and routes</read_first>
  <action>Fill any gaps in tests/test_api.py so the 03-01 stubs run against TestClient with-block with the real loader pointed at the tmp registry or a monkeypatched load_serving_artifacts returning a fitted tiny pipeline plus stub estimator, plus a mocked SQLAlchemy Session asserting session.add received all 24 fields with prediction, probability, model_version and exactly one commit per request. Run the API file then the full suite from the project root, fixing only regressions attributable to this plan, and record the FR-4.4 D-02 deviation plus the DATABASE_URL-unset 503 decision in the plan SUMMARY for downstream plans.</action>
  <verify>
    <automated>.\venv\python.exe -m pytest tests/test_api.py -q</automated>
    <fails_when>Exit code is not 0, or the output contains the string FAILED, or the summary line shows any skipped stub.</fails_when>
    <automated>.\venv\python.exe -m pytest tests -q</automated>
    <fails_when>Exit code is not 0, or the output contains the string FAILED.</fails_when>
  </verify>
  <done>tests/test_api.py fully green; full suite green from project root; SUMMARY documents the deviation and the 503 decision.</done>
  <acceptance_criteria>tests/test_api.py reports at least 8 passed and 0 failed; full suite reports 0 failed; mocked-session test asserts model_version 9 on the added row; no test touches real mlruns or real Postgres.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client -> /predict | Untrusted JSON crosses into Pydantic validation before inference |
| API process -> MLflow registry | Model bytes loaded once at startup from pinned version |
| API process -> Postgres | Prediction rows written via ORM-bound parameters only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Spoofing/Tampering | POST /predict body | medium | mitigate | Pydantic v2 types plus Literal vocabularies reject bad input with automatic 422 before inference per D-06; no ge or le bounds so verified raw extremes pass |
| T-03-04 | Information disclosure | api/app.py error paths | medium | mitigate | Catch-all logs type name server-side and returns 500 JSON without traceback; DATABASE_URL never logged |
| T-03-05 | Tampering | model_loader startup load | low | mitigate | Pinned models:/corvus-ckd/<version> URI plus pipeline width assertion against n_features_in_ before serving; no per-request registry calls |
| T-03-06 | Denial of service | lifespan table creation | low | mitigate | create_tables retries three times then fails loud; Compose health-gated Postgres (03-04) removes the startup race in deployment |
</threat_model>

<verification>
End-to-end TestClient proof of predict plus health plus model_info with pinned version 9, then full suite green.
</verification>

<success_criteria>
Single happy path works end to end and is committed: valid 24-field request returns prediction plus probability plus model_version 9 and writes the full log row; invalid input gets 422; suite green.
</success_criteria>

## Artifacts this phase produces

Plan-scoped symbols (this plan):

| Symbol | Kind | File |
|--------|------|------|
| CKDRequest (24 required fields) | Pydantic class | api/schemas.py |
| PredictionResponse (prediction, probability, model_version) | Pydantic class | api/schemas.py |
| HealthResponse, ModelInfoResponse | Pydantic classes | api/schemas.py |
| REGISTERED_MODEL_NAME, resolve_pinned_version, load_serving_artifacts | constant plus functions | api/model_loader.py |
| PredictionLog, get_engine, create_tables | ORM class plus functions | api/db.py |
| lifespan, POST /predict, GET /health, GET /model_info | app plus routes | api/app.py |

<output>
Create `.planning/phases/03-prediction-api-containerization/03-02-SUMMARY.md` when done
</output>
